version: '3.7'
services:
    # Starts an NGINX server that serves front-end components
    source-hasher:
        build: "./source-hasher"
        image: "source-hasher"
        ports:
            - "8080:80"
        command: ["python3", "app.py"]
        # restart: "always"
